version: '3.8'
services:
  metube:
    image: alexta69/metube
    container_name: metube
    restart: unless-stopped
    ports:
      - "8085:8081"
    volumes:
      - ./downloads:/downloads
    env_file:
      - .env
